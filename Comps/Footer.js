const Footer = () => {
    return (
        <footer>
            <p>Copyright 2021 Plant List</p>
        </footer>
    );
}

export default Footer;

//these are called reusable dropping components (both the navbar and the footer)